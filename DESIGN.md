# DESIGN.md

ThinkMove GPT-image2 Slide Systemの視覚設計の正本。

このファイルはCSS実装の仕様ではない。GPT-image2へ「完成した1枚のスライドをどう描かせるか」を伝える。

## 1. 画面の目的

> 複雑な状況を、重要な論点、判断材料、叩き台、次の一歩へ変換する。

見栄えだけのポスターを作らない。各ページには、数字、根拠、手順、比較、保留、次の一手のいずれかを置く。

## 2. 出力形式

- 16:9の完成ラスタ画像
- ロゴ、人物、日本語、図解を含めて一括生成する
- HTML、SVG組版、ネイティブ図形、文字後載せ、ロゴ後合成を使わない
- 後から埋める空白やプレースホルダーを作らない
- 不合格ページは局所パッチせず、短い指示で再生成する

## 3. 配色

| Role | Color | 用途 |
|---|---|---|
| Ink | `#0a1628` | ダーク面、最重要の文字、強い結論 |
| Navy | `#1e3a5f` | 見出し、線、主要な構造 |
| Teal | `#0d9488` | 前進、選択、次のアクション |
| Orange | `#f97316` | 1点だけの注意、変化、限定強調 |
| Background | `#fafbfc` | 基本キャンバス |
| Body | `#475569` | 本文、補足 |

画面比率の目安は、白70% / Navy 15% / Gray 10% / Teal 4% / Orange 1%。Orangeを広い背景やCTAへ使わない。

機械可読な値は[`brand/theme/design.json`](brand/theme/design.json)を参照する。

## 4. 情報階層

1. 結論型タイトル
2. 主張を支える証拠または構造
3. 判断を変える注目点
4. 出典、条件、保留

- 1ページの主論点は1つ
- 本文は3〜5要素を目安にする
- 小さなカードを大量に敷き詰めない
- 色だけで意味を伝えない
- タイトルだけを順に読んでも論理が追えるようにする

## 5. 構図

- 視線が「問い→証拠→判断」または「現状→変化→次の一手」と流れる
- 人物や装飾を主張より前に出さない
- ロゴのために大きな空白を確保しない
- 同じ構図を意図なく3ページ以上続けない
- 表紙だけ別ブランドのように派手にしない
- 余白と情報不足を混同しない

## 6. visual family

最初に基準ページを1枚生成し、合格した画像を後続ページのstyle referenceにする。

- primary tasteは1つだけ選ぶ
- palette、線、陰影、人物比率、余白、情報密度を揃える
- 全tasteの画像やcontact sheetを生成モデルへ同時に渡さない
- 各ページには、選択したtasteの個別previewだけを使う

用途別の選択は[`tastes/README.md`](tastes/README.md)を参照する。

## 7. 参照素材

`assets/references/`に残すのは、生成モデルへ直接渡せるラスタ参照だけ。

- ThinkMoveロゴ
- 豊藏翔太のプロフィール写真
- 公開許諾済み顧客ロゴ
- 4コマの画風参照

参照素材は最終画像へ貼り付けない。モデルが描くための入力として使う。完全一致が完了条件なら、編集可能な制作方式へ切り替える。

## 8. 禁止

- 架空の数字、引用、顧客、成果、UI
- 読めない日本語を「雰囲気」として残す
- unrelated logo、watermark、署名
- 汎用的なAI回路、脳、光る球体だけで説明する
- 情報の薄い美しいポスター
- 複数のテイストを平均化した無個性な画面

## 9. 完成判定

- 3秒でページの役割が分かる
- 30秒で「何を判断し、次に何をするか」を説明できる
- sourceと主張・数字・固有名詞が一致する
- デッキ全体が同じvisual familyに見える
- [`system/qa-checklist.md`](system/qa-checklist.md)をすべて確認している
